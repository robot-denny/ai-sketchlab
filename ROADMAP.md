# Roadmap

The project-level queue. Each entry is a slug, a one-line intent, and a pointer to the feature folder, PRD, or "no spec yet."

Per-feature increments (shipped + planned within a feature) live in the **Increments** section of each `_features/<slug>.md`. This file tracks work *across* features and the things that don't yet belong to a feature.

When a body of work spans three or more features and needs a shared intent doc, write a PRD at `_prds/<slug>.md` and link it from the **Bundles** section below.

---

## Now

In-flight features — actively iterating; each has a non-empty Increments queue in its feature doc.

_Nothing actively in-flight._ The **Pillar 2 architecture push** (the agency-standard RCL split + folder-by-kind migration that occupied this section through June) **completed 2026-06-30** — see the `arch-*` entries under Recently shipped. Its deferred follow-ons (obsolete-API migration, view nullable hardening, the optional view-tier migration) are in **Next**; the full standing rationale lives in CLAUDE.md → Solution architecture.

## Next

Committed work, no spec yet. Promote into a feature folder when starting. Includes the deferred follow-ons from the (now-complete) Pillar 2 push and natural between-slices fill-in work.

- **cloud-image-generator-launch-path** — *regression discovered during `fix-e2e-dev-only-failures` Step 1 (2026-05-29).* The image generator CLI fails to launch on Cloud's Windows runtime container before any OpenAI call: worker PATH has only ancient Node 0.10.28 (no npx); modern Node lives at `C:\Program Files\nodejs\<version>\` but isn't on PATH; even with `ImageGenerator__NodeBinPath` set, [CliImageGenerator.cs:73](src/HelloWorld/CliImageGenerator.cs#L73) probes `Path.Combine(path, "npx")` without `.cmd`/`.exe` extension, so Windows resolution always misses; and `scripts/image-generator/node_modules/` is gitignored + Cloud has no `npm install` step ([[project_cloud_build_no_npm]]). Spec captures the 4-layer root cause; plan will decide vendoring vs. CI-side `npm install` for the `node_modules` deployment. `dashboard.spec.ts:141` is currently `test.skip`-ed on Cloud URLs until this lands → `_specs/cloud-image-generator-launch-path.md`
- **fix-member-role-assign-local-sqlite** — *surfaced 2026-06-29 during `arch-migrate-premium-role-handler` Step 3 runtime verification.* On **local (SQLite)**, saving a new Member fires `MemberSavedNotification` → `AssignMembersToPremiumRoleHandler` runs and calls `IMemberService.AssignRole(id, "H5YR")` — confirmed by the `Automatically assigning member {id} to role H5YR` log line — but the assignment **does not persist**: the member shows `groups: []` afterward (verified via Management API *and* the backoffice). On **Live (SQL Server)** the same save correctly adds the member to `H5YR`. Pre-existing and environment-specific; **not** caused by the RCL relocation — the handler provably executes, so the gap is downstream in Umbraco's core member/DB layer (suspected local single-writer SQLite/WAL transaction-visibility behavior when assigning a role from within a saved-notification). The `H5YR` member group exists on local, so the group's absence is not the cause. Dev-environment annoyance, not a production bug. Investigate the SQLite/notification-scope mechanism only if local member-role testing becomes necessary → no spec yet
- **fix-backoffice-search-beta3-fieldname-crash** — *upstream beta bug, triaged 2026-06-05.* Typing in the backoffice **Media** (and almost certainly **Content**) list-view search box throws `'field name' cannot be null or empty` from Examine: `Umbraco.Cms.Search.Provider.Examine`'s `Searcher.SearchAsync` builds a `GroupedOr` query with an empty field name (chain: `ByKeyMediaCollectionController` → `MediaListViewService` → `ContentSearchServiceBase.SearchChildrenFromIndexAsync` → the beta provider). Root cause is the `Cms.Search.BackOffice` beta.3 `AddBackOfficeSearch()` registration overriding Umbraco 17's built-in list-view search. **Interim workaround already applied:** [SearchComposer.cs](src/UmbracoProject/SearchComposer.cs) no longer calls `AddBackOfficeSearch()`, so list-view search reverts to built-in Examine (works). The real fix — re-enabling the framework-backed backoffice search — is blocked on an `Umbraco.AI.Search` build compatible with `Cms.Search.* beta.4+` (the beta.3 pin is load-bearing; see *Pinned betas* in CLAUDE.md). Revisit during the v18 upgrade work. Pure config; no public-site impact → no spec yet
- **fix-imagecarousel-first-image-picker** — *test brittleness surfaced during `fix-e2e-dev-only-failures` Step 3 (2026-05-29).* [imageCarousel.spec.ts:337](tests/e2e/blocks/imageCarousel.spec.ts#L337) picks the first 3 images by depth-first tree-walk order — any new media upload that lands as "first" silently re-breaks the `:742` alt-text assertion (and likely sibling assertions). Today's fix (setting `altText` on Codegarden keynote + Say Cheese) is content remediation that will rot the next time the media tree reshuffles. Proper fixes: (a) seed a known media item with known altText in `beforeAll` and pick that, OR (b) set `altText` on the picked media inside `beforeAll` with `afterAll` restoration (mirrors the existing contentRows save-and-restore). Pure test change, isolated to one file → no spec yet
- **fix-article-list-block-bypass** — *regression.* The v2 Article List redesign ([articleList.cshtml](src/UmbracoProject/Views/articleList.cshtml)) renders articles directly as an `.entries` grid of `_ArticleCard` partials and no longer renders the page's `contentRows` block list — so the `latestArticlesRow` block's `displayMode` (list ↔ grid) toggle is orphaned and unrendered on Article List pages (the block data still sits in `contentRows`). Decide: restore `contentRows` rendering on `articleList.cshtml`, or formally retire the block and clean the orphaned data from the Article List page. The Browser E2E section of [tests/e2e/articleListGridView.spec.ts](tests/e2e/articleListGridView.spec.ts) is `describe.skip`-ed pending this — un-skip and re-point (or delete) when resolved. Surfaced 2026-05-27 during the E2E-rot repair → no spec yet
- **editor-how-to-guides-iteration** — v1 shipped (see Recently shipped); follow-on only: iterate on AI-generated guide output and page-type options → [_features/editor-how-to-guides.md](_features/editor-how-to-guides.md). Deprioritised from Now
- **living-style-guide-additions** — v1 shipped (see Recently shipped); follow-on only: ad-hoc additions as new editorial classes are introduced → [_features/living-style-guide.md](_features/living-style-guide.md). Deprioritised from Now
- **arch-uda-ci-guard** — promote the [.githooks/pre-commit](.githooks/pre-commit) `.uda` drift check into a CI step so contributors without the opt-in hook (activated via `git config core.hooksPath .githooks`) are still covered. Builds on the existing `/check-uda` skill logic. Closes the opt-in fragility surfaced during the `arch-safety-net` spec walkthrough — naturally clusters with the CI work that bundle ships. **Also fold in a `dotnet publish -c Release` gate** (Gate 1 and/or the pre-push hook currently run only `dotnet build` + `dotnet test`): publish-only failures are invisible to `build` and reach Cloud unchecked. This bit us on 2026-06-29 — the RCL+host duplicate Umbraco schema files collided as `NETSDK1152` *only* on `dotnet publish`, passing every build gate but failing the Kudu Portal→Live promotion (see `[[project_rcl_host_schema_publish_collision]]`). A publish step in Gate 1 would have caught it pre-Cloud → no spec yet
- **ci-gate2-search-warmup-poll** — *recurring post-deploy footgun, hit again 2026-06-29 (master run 28396498415).* The `cloud-deployment` (Dev) job reports "finished" before the deployed app is fully warm, so the `playwright-against-dev` job can launch while `Umbraco.AI.Search` is still initialising. Symptom: `/search` returns 0 results **and** `POST /document` 500s (AI.Search hooks the publish pipeline to embed each doc, so an un-warm embedding path throws on publish) — cascading into ~10 spurious failures across `search`/`search.screenshot`, `articleCardMetaDescription`, `ellaBlockAttribution`, `experiments.blockgrid`, `guides-cli`, `sectionNavigation`. The index data is fine (179 docs); the subsystem just isn't serving yet. Today's manual fix was a Dev restart + `gh run rerun --failed`. Durable fix: add a warmup gate between `cloud-deployment` and Playwright that polls `GET /search?q=article` (against `vars.URL`) until it returns ≥1 `article-grid-card`, with a bounded timeout, before launching the e2e job — so a cold deploy self-heals instead of needing a hand restart. Clusters with the Gate-2 CI work in `arch-uda-ci-guard`. `fix-infra` (no feature doc); durable record → [CI Failure Recipes runbook](docs/ci-failure-recipes.md) → no spec yet
- **arch-obsolete-api-migration** — migrate Razor views (and any C# helpers) off Umbraco APIs marked `[Obsolete]` for v18, principally `IPublishedContent.Children` → `IDocumentNavigationQueryService.TryGetChildrenKeys()`. As of `arch-modelsbuilder-source-mode` (2026-06-29) `CS0618` is **no longer** project-wide-suppressed — it's a build-time gate, and each existing obsolete call site is grandfathered with a scoped per-call-site `#pragma warning disable/restore CS0618`. So this ticket = adopt the new APIs and **remove the grandfathering pragmas one file at a time** (the build then proves each file is clean). Naturally clusters with the v18 upgrade work. Starting file: `Views/Partials/v2/_SiteHead.cshtml` (pragma at lines 37–41); expect ~5–10 affected files total → no spec yet
- **arch-view-nullable-hardening** — *surfaced 2026-06-29 during `arch-modelsbuilder-source-mode` Step 2.* Enabling build-time Razor compilation made the C# compiler check ~102 pre-existing nullable-reference-type sites across 22 views (`.Url()`, `.Children<T>()`, etc. — idiomatic Umbraco template code that was never nullable-checked under the old runtime-compile model and runs fine). Grandfathered for now via a project-wide `<NoWarn>CS8600;CS8602;CS8603;CS8604</NoWarn>` on `UmbracoProject.csproj` (chosen over ~22 files of per-block pragmas because the host C# that most benefits from strictness is migrating into the `UmbracoProject.Features` RCL, which keeps `<Nullable>enable</Nullable>` — the lasting gap is only `Program.cs` + a few `Helpers`). This ticket re-hardens views incrementally — add explicit null handling per view, then remove the suppression — restoring nullable enforcement. Sibling to `arch-obsolete-api-migration`; naturally clusters with the v18 upgrade work → no spec yet
- **arch-feature-folder-views-tier** — *optional follow-on, declined for now (Pillar 2 step (5)).* Migrate Razor views into the RCL (`Blocks/<El>/Views/`, `Pages/<Page>/Views/`), which requires building the reference's `ViewModelFactory`/`TemplateCoordinator`/route-hijacking-controller rendering framework. Declined during the Pillar 2 push as parity-for-parity (low marginal resilience over stock Umbraco block/template conventions + the already-extracted `SearchService`); views stay in the host's stock `Views/`. Pursue only if pages accrete logic that justifies it → no spec yet
- **workflow-explore** — `/explore <idea>` divergent pre-spec command, inspired by Pocock's [`/grill-me`](https://github.com/mattpocock/skills/blob/main/skills/productivity/grill-me/SKILL.md). Holds a Three-Amigos-style conversation, writes notes to `_specs/<slug>-explore.md`, segues into `/spec`. Part of the 2026-05-13 workflow bundle → no spec yet
- **section-nav-hide-toggle** — extracted from the polish bundle. New `hideFromSectionNavigation` boolean alongside the existing `umbracoNaviHide` / `hideFromTopNavigation` / `hideFromXMLSitemap` toggles in the Visibility Controls composition; `sectionNavigation.cshtml` filters by it. Earns a new Increment on `_features/section-navigation.md` → run `/spec section-nav-hide-toggle` from master
- **cleanup-contact-dead-code** — remove the unused `Views/Components/Contact/Default.cshtml` view component. Never invoked (the live form renders via Umbraco Forms' `RenderForm`). Five-minute housekeeping; natural between-slices work during the Pillar 2 migration
- **a11y-batch-2-forms-semantics** — Umbraco Forms / member-registration accessibility: visible labels (`input_label_visible`), `<fieldset>` grouping (`input_fields_grouped`), submit-button presence (`form_submit_button_exists`), plus heading semantics (`text_block_heading`) and the single missing accessible link name (`a_text_purpose`). Overlaps the Contact/Forms surface with `cleanup-contact-dead-code` above. Batch 2 of the accessibility remediation (Batch 1 shipped) → `_specs/accessibility-fixes.md` (Full audited inventory / Proposed batches)
- **a11y-batch-3-content-landmark-sweep** — content & landmark sweep: wrap stray content in landmarks (`aria_content_in_landmark`, 69), inline quote semantics (`text_quoted_correctly`, 20), blockquote attribution (`blockquote_cite_exists`, 3), and unobscured focus targets (`element_tabbable_unobscured`, 6). Batch 3 of the accessibility remediation → `_specs/accessibility-fixes.md` (Full audited inventory / Proposed batches)
- **a11y-batch-4-long-tail** — accessibility long tail: sensory-language instructions (`text_sensory_misuse`), 200% viewport resize (`style_viewport_resizable`), background-image alt (`img_alt_background`), deprecated attributes, Windows high-contrast mode, scripted links (`script_onclick_avoid`), complementary-region label, `<figure>` label (`figure_label_exists`), new-window warning (`a_target_warning`), and redundant ARIA role. Batch 4 of the accessibility remediation → `_specs/accessibility-fixes.md` (Full audited inventory / Proposed batches)
- **workflow-retire-block-command** — fold [.claude/commands/block.md](.claude/commands/block.md) into a `/plan` template variant so block-shaped TDD stops being a separate command. Reduces command surface and re-uses the spec → plan → implement chain. Part of the 2026-05-13 workflow bundle → no spec yet
- **workflow-roadmap** — `/roadmap` command to manage [ROADMAP.md](ROADMAP.md) (promote/demote items between Now/Next/Later/Recently shipped, append Increments entries to feature docs). Currently a hand-edit chore. Part of the 2026-05-13 workflow bundle alongside [shipped `/implement-step`](.claude/commands/implement-step.md) → no spec yet

## Later

Wishlist. Promote into Next when scope and timing become clearer.

- **hero-block** — new block component. v1: image + text + button. Later: backoffice text positioning, background-video variant, layout configuration choices → `_features/hero-block.md` (no spec yet)
- **workflow-bundle-mode** — ✅ **superseded 2026-06-16** by the three-way **work-type classification** added to `/spec`, `/plan`, `/feature` (see CLAUDE.md → Workflow layers → "Work types — which artifacts a piece of work earns"). The original idea was a binary `--bundle` mode to suppress the hard-coded feature-doc step; the classification generalises it: `new-capability` creates a feature doc, `change-to <existing>` folds into the existing capability doc, `fix-infra` creates none (durable residue → `docs/` runbook / CLAUDE.md). The 2026-05 audit's P1/P2 hygiene cluster classifies as `fix-infra`. Remaining (optional) follow-on: auto-detect the work type instead of relying on the `**Work type**:` line being set by hand
- **workflow-prd** — lightweight `/prd` command for authoring multi-feature PRDs in `_prds/`. Currently a free-form markdown convention. Part of the 2026-05-13 workflow bundle; promote to Next when a bundle that needs a PRD (e.g. `algorithmic-art-platform`) reaches active scoping → no spec yet

## Bundles

Multi-feature initiatives. Each gets a PRD when work begins.

- **algorithmic-art-platform** — extend the existing `/algorithmic-art` skill into a first-class capability: backoffice trigger, password-protected user-facing prompt page, gallery URL for saved pieces. Likely 3-4 features under one shared intent → `_prds/algorithmic-art-platform.md` (no PRD yet)

---

## Recently shipped (for orientation)

Rolling **60-day window**, newest last. Each entry is name + date + a one-line gist; the full behavioral contract lives in `_features/<slug>.md` and the why/how in `_specs/shipped/` + `_plans/shipped/`. Shipped work older than the window is dropped from this list but stays discoverable via those docs.

- [editor-how-to-guides](_features/editor-how-to-guides.md) — 2026-05-04 — **Guides** section + `howToGuidePage` doc type; `/guide` command generates/amends per-feature how-to pages via the backoffice AI Agent (drift-detects, never overwrites editor work).
- [ella-block-attribution](_features/ella-block-attribution.md) — 2026-05-11 — attribution surface for the Ella block.
- **site-polish-2026-05** — 2026-05-12 — 5-item cleanup bundle (listing metaDescription, Generic-tab composition fix, Contact-form + Notes-block restyle, capabilities-doc refresh). No feature doc.
- [innovation-showcase](_features/innovation-showcase.md) — 2026-05-13 — `/experiments` landing page; the project's first production **Block Grid** layout (areas, multi-column + nested rows), telling the seven-pillar story end-to-end editable.
- **workflow-implement-step** — 2026-05-19 — `/implement-step <plan> N` dispatches a single plan step to a fresh subagent (Context + Key Decisions + Step N only; no auto-commit). See [.claude/commands/implement-step.md](.claude/commands/implement-step.md).
- [extract-search-service](_features/umbraco-ai-search.md) — 2026-05-20 — refactored `search.cshtml` business logic into a composer-registered `SearchService`; added the xUnit project + search-mode routing tests. First of the two Pillar 2 logic-in-view extractions.
- **arch-safety-net** — 2026-05-27 — the CI/refactor safety net: two-gate Cloud CI/CD Flow pipeline (build+xUnit local, Playwright vs Dev post-deploy), pre-push hook, `<TreatWarningsAsErrors>` + narrow `<NoWarn>`, 27 block + 6 page screenshot specs, `## CI/CD & Build hygiene` in CLAUDE.md. Surfaced the `Version="*"` and no-npm Cloud gotchas (now project memories). No feature doc.
- [arch-image-generator-extraction](_features/image-generator.md) — 2026-05-28 — replaced the CLI-shellout in `ImageGeneratorController` with an `IImageGenerator` seam (`CliImageGenerator`, composer-registered, unit-testable). Closed the second Pillar 2 logic-in-view weakness.
- [fix-e2e-dev-only-failures](docs/ci-failure-recipes.md) — 2026-05-29 — restored the Playwright-vs-Dev gate from perpetually-red to mostly-green (guides-cli env fix, carousel altText content fix); deferred the Cloud image-generator launch failure to `cloud-image-generator-launch-path`. Seeded the "Diagnosing a red CI run" playbook + [CI Failure Recipes runbook](docs/ci-failure-recipes.md).
- [remove-seotoolkit](_features/seo-routing.md) — 2026-06-02 — dropped the deploy-incompatible `SeoToolkit.Umbraco` package; replaced its value with in-tree code on the normal pipeline (`/sitemap.xml` rewrite middleware, `/robots.txt` static file, branded 404 via `NotFoundContentFinder`). Smoke spec locks it; `## SEO Routing` in CLAUDE.md documents the wiring.
- **fix-screenshot-baselines** — 2026-06-04 — root-caused the 26 perpetually-red Gate 2 failures to the `arch-safety-net` screenshot specs shipping with **no committed Linux baselines** (and `update-snapshots.yml` itself broken two ways). Generated + committed baselines; recurrence guard captured as a project memory. No feature doc.
- [umbraco-ai-search](_features/umbraco-ai-search.md) — 2026-06-16 — migrated the AI + search stack to **stable 1.0.0** (Site Search originally shipped 2026-04-22). Evergreen behavior folded into the Site Search feature doc; ACs in `_specs/shipped/migrate-ai-search-stable-1-0.md`.
- **arch-feature-folder-architecture** — 2026-06-19 — **Pillar 2 push, pilot.** Stood up the agency-standard two-project **RCL split** (`UmbracoProject.Features` + thin host, folder-by-kind) and proved it by migrating the **Search** slice behavior-preserving; cross-assembly composer auto-discovery runtime-verified. Durable record in CLAUDE.md `## Solution architecture`. No feature doc (`fix-infra`).
- **arch-modelsbuilder-source-mode** — 2026-06-29 — **Pillar 2 push, gate (1).** Switched ModelsBuilder `InMemoryAuto` → **`SourceCodeManual`** (75 models committed in the RCL so model-coupled C# can build-time-compile), turned build-time Razor compilation back on (making `CS0618` a build gate; cleanup → `arch-obsolete-api-migration`), and grandfathered ~102 view nullable warnings (cleanup → `arch-view-nullable-hardening`). Unblocked model-coupled migration. CLAUDE.md `## ModelsBuilder`.
- **arch-migrate-premium-role-handler** — 2026-06-29 — **Pillar 2 push, step (2).** Moved `AssignMembersToPremiumRoleHandler` → RCL `Infrastructure/` + its composer → `Composer/`, behavior-preserving. (Surfaced the unrelated local-SQLite role-persistence quirk now tracked as `fix-member-role-assign-local-sqlite` in Next.)
- **arch-migrate-routing-infra** — 2026-06-30 — **Pillar 2 push, step (3).** Moved the cross-cutting routing/SEO infra into the RCL: `NotFoundContentFinder` + composer → `Infrastructure/ContentFinder/`, and the `/sitemap.xml` → `/xmlsitemap` rewrite out of `Program.cs` into `SitemapRewriteMiddleware` (`app.UseSitemapRewrite()`).
- **arch-migrate-helloworld-clusters** — 2026-06-30 — **Pillar 2 push, step (4) — decided to *keep*, not move.** HelloWorld's ImageGenerator/Palettes C# stays in HelloWorld: it's a self-contained packaged backoffice extension, not site business logic, and relocating would split a cohesive feature + add a `HelloWorld → RCL` dependency for no resilience gain. Rationale in CLAUDE.md → Solution architecture.
- **arch-feature-folder-migration** — 2026-06-30 — **Pillar 2 push complete.** With steps (1)–(4) shipped/decided and the optional view-tier (5) deferred to Next (`arch-feature-folder-views-tier`), the per-slice migration that adopted the agency-standard architecture is done. Pillar 2 (architectural separation) moved 2 → 4. No feature doc (`fix-infra`).

---

**How to use this file:** add to Now / Next / Later as items appear in your head. Promote between sections as priorities shift. When an item ships, move it under "Recently shipped" and let the feature doc carry the detail.

**Workflow entry point:** for a single feature, run `/spec <slug>` to converge an idea into a spec. The spec command's "Next:" line walks you through `/plan` → implement → `/feature update` → `/code-review`. See [CLAUDE.md → Workflow layers](CLAUDE.md#workflow-layers) for the full chain.
