- [Backoffice login loop after Playwright e2e runs](project_backoffice_lockout_after_playwright.md) — userNoConsole=1 from hitting the 5-failed-attempt lockout. Skip the OAuth/cookie forensic dive; one SQL reset clears it.
- [Cloud Deploy bootstrap silently skips schema import](project_cloud_deploy_bootstrap_skip.md) — Live Deploy dashboard with "hide up to date" toggled exposes per-state actions (Update item for mismatch, Create for orphan); restart and empty-commit nudges usually do nothing.
- [ai-context Resource IDs are Cloud-managed](project_ai_context_resource_ids_are_cloud_managed.md) — never hand-edit/feature-branch the Resources[].Id GUIDs in umbraco-ai-context .uda; clobbering them hard-fails cloud-sync apply_patch.sh (broke master #42/#43). The sync patch is PINNED to the last-green deploy and served unchanged; to unstick, restore the affected files to the patch BASE (last-green-deploy commit), NOT Cloud's current value — let the patch apply forward.
- [umbraco-cloud.json must stay git-tracked](project_umbraco_cloud_json_must_stay_tracked.md) — never untrack it (breaks the deploy pipeline + Dev load); keep it git-tracked AND stock all-workspaces (Dev+Live), no Live-only divergence — the local→Dev→Live content model removes the only reason anyone kept one. Recovery when the pipeline is down: re-track + direct FF push to the env's Cloud SCM remote (writes the env's own copy; the CI/CD Flow path preserves it and won't). Confirmed 2026-07-10.
- [Content authoring direction (local → Dev → Live)](project_content_authoring_direction.md) — content originates locally but flows local → Dev → Live through the pipeline (not direct to Live); do not assume Cloud is source of truth for content records. Runbook: docs/content-transfer-workflow.md.
- [Content transfers/restores are ADDITIVE](project_content_transfers_are_additive.md) — local deletions never propagate to Live; a name collision with a stale target node creates a "(1)" duplicate at slug `-1` that breaks hardcoded links. Reclaim a clean slug by deleting BOTH the orphan and the "(1)" on Live, clear URL-Tracker 301s, re-transfer, then restore Live→Dev.
- [Push to `github`, not `origin`](project_git_push_to_github_not_origin.md) — `origin` is Umbraco Cloud Live SCM; `github` is the real dev repo. Use `git push -u github <branch>`; PRs against robot-denny/ai-sketchlab.
- [TipTap Style Select customization in Umbraco 17](project_tiptap_style_select_customization.md) — TinyMCE's `umb_name` CSS annotations are dead; declare entries in a `styleMenu` manifest in HelloWorld bundle and `overwrites: 'Umb.Tiptap.Toolbar.StyleSelect'` so no `.uda` edit is needed.
- [Cloud shared secrets are one-shot](project_cloud_shared_secrets_one_shot.md) — Umbraco Cloud's shared/project-wide secrets must be set up from the start; cannot be retroactively converted from per-env without rotating actual API keys.
- [Cloud rejects wildcard package versions](project_cloud_no_wildcard_versions.md) — `Version="*"` in PackageReference passes local build but fails Cloud's CI/CD Flow deploy validator. Pin explicit versions; align Umbraco.Cms.* across projects.
- [Cloud's build container has no npm](project_cloud_build_no_npm.md) — Kudu Lite container builds in Debug without `CI=true`. Common skip-conditions don't catch it. Add `ContinueOnError="true"` to MSBuild `Exec` for `npm` calls; commit the build output.
- [Cloud's runtime Razor compile honors TWAE](project_cloud_razor_honors_twae.md) — `[Obsolete]` API calls in .cshtml fail as `CS0618` on Cloud's first-request Razor compile because Roslyn at runtime picks up the project's TWAE setting. Local dev hides this via cached compiled Razor. Suppress `CS0618` until the migration off obsolete APIs ships.
- [Cloud's runtime Razor ignores csproj NoWarn](project_cloud_razor_ignores_nowarn.md) — `<NoWarn>` in csproj doesn't reach Cloud's runtime Razor compile (even though TWAE does). Suppress per-file with `#pragma warning disable` directives at the call site instead. Best practice: refactor the call into a `@{}` block scoped with pragmas.
- [AI copilot object-cycle on Extended Markdown Editor pages](project_ai_markdown_serialization_cycle.md) — Umbraco.AI's content serializer returns MarkdownValue as-is → Markdig AST `.Parent` cycle → depth-64 crash. Fixed via a JsonConverter appended to Umbraco.AI's STATIC DefaultJsonSerializerOptions (DI options aren't consulted). Updating the editor doesn't fix it and needs Umbraco 18.
- [AI $-config-reference allowlist (current 17.x suite; landed in 1.14.0)](project_ai_1_14_allowed_config_key_prefixes.md) — AI.Core rejects `$OpenAI:ApiKey`/`$Anthropic:ApiKey` unless their prefix is in `Umbraco:AI:AllowedConfigurationKeyPrefixes`; embedding error is swallowed (`Vector search failed for index UmbAI_Search` log) → semantic /search 500s. Fix in CLAUDE.md → "AI config-key allow-list": re-list the two defaults + add OpenAI/Anthropic in committed appsettings.json.
- [Gate 2 transient deploy/Playwright flakiness](project_gate2_transient_deploy_flakiness.md) — four non-code failure modes: status-poll 500 & 503 sanity-curl warmup → `gh run rerun <id> --failed`; terminal verify-timeout `DeploymentStatus: Failed` → FULL-workflow `gh run rerun <id>` (--failed only re-polls the already-dead deployment); cold-AI.Search `POST /document` 500 cascade (`/search` = "No matches") → **Portal restart of Dev** (NOT an index rebuild), then `--failed`. Confirm Dev serves 200 first.
- [Reproducing screenshot failure = broken test, not transient](project_gate2_screenshot_reproducing_is_broken_test.md) — a Gate 2 screenshot diff that reproduces identically across reruns is deterministic (broken query/guard), NOT flaky; investigate the diff, don't `--failed`. Regen baselines only via `update-snapshots.yml` after confirming the change is intentional.
- [MCP env vars come from launching shell, not .env](project_mcp_env_not_from_dotenv.md) — umbraco-mcp -32000 / "Missing environment variables" means creds aren't exported in Claude's shell; .mcp.json ${VAR} expands from the process env, never .env. Launch via the `claude-umb` alias. Also: site must be serving on :44367 first, and the VSCode extension only has MCP if VSCode itself was launched from an env-loaded shell.
- [RCL+host duplicate Umbraco schema → NETSDK1152 on publish only](project_rcl_host_schema_publish_collision.md) — RCL and host both emit appsettings-schema*.json/umbraco-package-schema.json; builds fine, collides on `dotnet publish` (NETSDK1152). Invisible to `dotnet build`/Gate 1; fails Cloud Kudu Portal→Live promotions (not GitHub-Actions Dev). Fix: `<ErrorOnDuplicatePublishOutputFiles>false</>` on host. ALWAYS `dotnet publish` to verify RCL-split changes.
- [Umbraco.AI suite realigned to CMS-17 line (17.0.0)](project_ai_suite_17_version_alignment.md) — move whole AI stack to 17.0.x on Cms 17.5.x (fixed login-label regression + copilot AG-UI); AI.Search 17.0.0 pins Cms.Search.Core to 1.x (keep it there).
- [AI copilot large-output ceiling](project_ai_copilot_large_output_ceiling.md) — set_value replaces whole value, so editing a long RichText block forces a huge output that silently aborts the stream (~22s). Keep blocks section-sized; only knob is profile MaxTokens.
- [C# Dev Kit silently drops UmbracoProject from the .sln](project_csdevkit_removes_project_on_fast_git_rewrite.md) — after a squash merge, local `checkout` + `ff-merge` rewrites the csproj twice in one second; C# Dev Kit calls `removeProjectViaPath` and strips the test ProjectReference. False green: build "succeeds" and 70/70 tests pass because the host isn't in the build. Always `dotnet sln list` (expect 4) before trusting a green build.
- [umbracoNaviHide's label lies](project_umbraconavihide_label_lies.md) — it drives section nav + sitemap, NOT site search (search filters by doc-type alias only); hide a subtree by ticking the top node only, since the sitemap never descends into a hidden node.
- [Razor `string?` fails runtime compile (CS8669)](project_razor_nullable_annotation_cs8669.md) — nullable annotations in .cshtml build clean but 500 the page once the file mtime moves; RuntimeCompilation arrives transitively via Umbraco, so grepping csproj says "off" and is wrong. Use an empty-string sentinel.
